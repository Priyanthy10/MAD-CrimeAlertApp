import MapView, { Marker, Circle, Heatmap } from 'react-native-maps';

export const Map = MapView;
export const MapMarker = Marker;
export const MapCircle = Circle;
export const MapHeatmap = Heatmap;

export default MapView;
